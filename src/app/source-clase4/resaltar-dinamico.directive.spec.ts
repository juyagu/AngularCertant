import { ResaltarDinamicoDirective } from './resaltar-dinamico.directive';

describe('ResaltarDinamicoDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarDinamicoDirective();
    expect(directive).toBeTruthy();
  });
});
