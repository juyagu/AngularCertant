import { ResaltarInputDirective } from './resaltar-input.directive';

describe('ResaltarInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarInputDirective();
    expect(directive).toBeTruthy();
  });
});
